## Summary

<!-- What changed, and why? -->

## Linked issue

<!-- Replace with the issue this PR resolves. Open an issue first for a non-trivial change. -->
Closes #

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run web:build` passes
- [ ] Behavior changes include tests (a bug fix includes a test that fails without the fix)
- [ ] Only example data in code, tests, fixtures, docs, and commit messages (constitution Principle I, `.specify/memory/constitution.md`)
- [ ] Web UI changes checked in a browser at desktop width and at 640px or narrower
- [ ] `README.md` / `CLAUDE.md` updated if behavior or conventions changed

## Manual verification

<!--
Required only when this change touches a network client that has no automated test:
Ssh2SSHClient or the real Authentik client, for example. Say what you verified against
real infrastructure, using example values rather than real hostnames or addresses.
Otherwise, write "n/a".
-->
n/a
